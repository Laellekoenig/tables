Rules for this project:

- All styling must be done using tailwind.
- Always use cursor-pointer for buttons
- Do not use one-line if statements, always use {} and at least three lines
- The components in /ui are shadcn components based on base ui

Assume the dev server is already running.
Never run build or dev.
Use tsc to check if project compiles.
